<?php

/**
 * Implements hook_ctools_plugin_directory().
 */
function custom_panels_style_ctools_plugin_directory($module, $plugin) {
  if ($module == 'panels' && $plugin == 'styles') {
    return 'plugins/' . $plugin;
  }
}
 
 
